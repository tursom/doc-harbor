services:
  doc-harbor:
    image: ghcr.io/tursom/doc-harbor:latest
    build: .
    init: true
    ports:
      - "14220:14220"
    environment:
      DATA_DIR: /data
      HTTP_ADDR: :14220
      # 留空不限制；填写逗号分隔的来源网段，限制所有 HTTP 页面和接口。
      ALLOWED_CLIENT_CIDRS: ${ALLOWED_CLIENT_CIDRS:-}
      WEB_DIR: /app/web/dist
      ALLOW_LOCAL_GIT: "0"
      ALLOWED_GIT_HOSTS: ""
      NETRC: /credentials/.netrc
      GIT_CONFIG_GLOBAL: /root/.gitconfig
      GIT_CREDENTIAL_STORE: /root/.git-credentials
      GITHUB_WEBHOOK_SECRET: ${GITHUB_WEBHOOK_SECRET:-}
    volumes:
      - ./data:/data
      - ~/.ssh:/root/.ssh:ro
      - ./credentials:/credentials:ro
      - ~/.git-credentials:/root/.git-credentials:ro
      - ~/.gitconfig:/root/.gitconfig:ro
